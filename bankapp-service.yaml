apiVersion: v1
kind: Service
metadata:
  name: bankapp-svc
  namespace: webapps
spec:
  type: NodePort
  selector:
    app: bankapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

